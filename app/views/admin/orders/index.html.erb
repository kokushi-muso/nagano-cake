<h1>注文履歴一覧</h1>

<table>
  <tr>
    <th>購入日時</th>
    <th>購入者</th>
    <th>注文個数</th>
    <th>注文ステータス</th>
  </tr>
  <tr>
    <% @orders.each do |order| %>
      <td><%= order.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
      <td><%= first_name(order.customer)+last_name(order.customer) %></td>
      <td><%= order.order_items.count %></td>
      <td><%= order.order_status %></td>
    <% end %>
  </tr>
</table>