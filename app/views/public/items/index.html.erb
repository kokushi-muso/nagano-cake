<div id="index-main-visual"></div>

<div class="text mt-5">
    <h1 class="site-title mb-3">商品一覧<span class="count">（全<%= @items_count.count %>件）</span></h1>
    <p>※商品画像をクリックすると、詳細をご覧いただけます。</p>
  </div>
</div>
  <ul class="flex-item">
    <% @items.each do |item| %>
    <% if item.is_active == true %>
    <li>
      <%= link_to item_path(item) do %>
        <div class="item">
          <%= attachment_image_tag item, :image %>
            <div class="mask">
            </div>
        </div>
        <h3 class="mt-3 mb-1"><%= item.name %></h3>
        <p>¥<%= (item.price_excluding_tax * 1.1).to_i.to_s(:delimited) %></p>
        <p><%= item.is_active ? "販売中" : "売切れ" %></p>
      <% end %>
    </li>
    <% else %>
    <li>
        <div class="item">
          <%= attachment_image_tag item, :image %>
            <div class="mask">
            </div>
        </div>
        <h3 class="mt-3 mb-1"><%= item.name %></h3>
        <p>¥<%= (item.price_excluding_tax * 1.1).to_i.to_s(:delimited) %></p>
        <p><%= item.is_active ? "販売中" : "売切れ" %></p>
    </li>
    <% end %>
   <% end %>
  </ul>

  <div class="paginate">
    <%= paginate @items %>
  </div>